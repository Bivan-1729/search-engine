#include <iostream>
#include <cassert>
#include "Trie.hpp"

int main() {
    Trie trie;
    // A simple assert verification step for checking CI setup
    assert(true); 
    std::cout << "All early verification tests passed!\n";
    return 0;
}